name: Eval smoke

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Smoke completed previously; use the full eval workflow for current runs."
