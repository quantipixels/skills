name: Eval provider probe

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hosted provider probes are disabled; the eval uses a pinned local open-weight model."
