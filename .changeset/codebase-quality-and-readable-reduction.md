---
"qp-skills": minor
---

Add existing-codebase quality assessments to `atunwo`, consolidating the experimental `codebase-grade` candidate into its core review workflow. Preserve contextual judgment, strengths, failure-path inspection, optional calibrated grades, and coverage limits without requiring a candidate acceptance verdict. Integrate assessment criteria into the shared evidence, challenge, and reporting steps without a separate assessment procedure. Update the review worker to support this result.

Require `pare` reductions and `atunwo` review recommendations to preserve or improve readability. Reject formatting removal and code compression as simplification, and preserve helpful explicitness.

Clarify idiomatic, readable minimum mechanisms and assembled identity/authority/resource-limit proof in `alaga`. Evaluate simplification across callers as well as implementation, audit cumulative compression pressure on actual skill loading paths, and assess whether generic return types have an enforced implementation relationship.

Align `pare` selection rules, relocation checks, helper examples, and findings with comprehension and maintenance benefits. Allow useful named helpers, longer clearer implementations, and justified retention without requiring structural counts to fall.
