---
"qp-skills": minor
---

Use each working tree's ignored `.qp/<owner-or-category>/` for local generated/project-support state, treating a linked worktree and its `.qp` as one candidate and reconciling only relevant state when that candidate is accepted elsewhere. Simplify file-producing skills to their natural destinations, let Atọ́nà record the active workspace and reconcile local state at closure, and let Pepeye hand accepted worker-worktree candidates back to the accepting workflow. Keep host-configuration backups inside the host's own data/config directory. Remove the obsolete generic QP settings/workspace model and unreleased Akọsílẹ̀ skill, runtimes, tests, and dedicated CI.
