---
"qp-skills": patch
---

Refine Experimental Fihàn around its user-facing serving outcome: when a bounded local resource is supplied, serve it through the narrowest eligible private transport and return the exact HTTPS URL or Tailcat receiver invocation. Resolve setup and CLI behavior from the installed tool's matching official documentation when present, otherwise from the latest official installation/setup documentation and current stable or upstream behavior, rather than hardcoding volatile recipes.
