---
"qp-skills": minor
---

Add an experimental Codex-only `qp-setup` skill for optional user or repository instructions and host settings. Bundle the general work instructions and offer a Pepeye binding, main-session defaults, and Context Notes independently. Require a concrete preview, permission, and backups; support scoped removal without replacing unrelated configuration. This is native-tool setup guidance, not the retired native-worker installer.
