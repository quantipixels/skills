---
"qp-skills": minor
---

Keep Pepeye focused on native-subagent coordination while restoring provider-specific worker model/effort preferences behind a selectively loaded provider reference. Put the exact Pepeye activation line in the skill itself so the same one-line contract can be pinned in `AGENTS.md`, `CLAUDE.md`, or equivalent host instructions. Add semantic, risk-based worker checkpoints with concise evidence-bearing reports, explicit coordinator steering actions, and a liveness backstop instead of a fixed two-minute polling cadence.
