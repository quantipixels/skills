---
"qp-skills": minor
---

Add explicit-only Experimental `orisun` for compact, exact-version upstream source grounding. It resolves the controlling project version before source lookup, prefers matching source and tests over upstream head, reports version/evidence gaps explicitly, and relies on native Git/search/provider capabilities instead of introducing a cache, CLI, MCP server, or search runtime.
