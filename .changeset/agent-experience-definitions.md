---
"qp-skills": minor
---

Simplify Pepeye delegation around assignment-synthesized workers. Remove the maintained work-posture catalogue and agent-definition renderer: each delegated worker is shaped directly from the actual assignment with the needed outcome, context, instructions, capability, execution boundary, independence, evidence, stop condition, and optional semantic skill. Prefer native/general host agents and per-spawn controls; keep provider-native agent definitions only as exceptional persistent configuration when a recurring capability cannot be expressed adequately through the assignment, native controls, or existing user configuration. Yorùbá worker names remain lightweight observability labels rather than routing/configuration identities. Keep `qp-setup` autonomous and audit-first, retain opt-in global-instruction auditing, and preserve the installation-aware updater for direct, Skills CLI, and Claude plugin installations.
