---
"qp-skills": major
---

Rebuild QP around direct outcomes, conditional engineering depth and inspectable human evidence. Fold decision interviews into atona and HTML construction into fihanmi; remove the separate arojinle/html-artifact identities, mandatory playbook machinery and bundled session adapter. Keep the Codex profile optional and preserve focused utility methods.

Replace the general eval campaign interface with reusable proof fixtures, independent oracles and native-host task protocols. Add model-free package CI with rejection tests and consistent explicit-only metadata for qp-update and pese. Preserve baseline experiment provenance and document migration/recovery limits. No measured model-performance improvement or native invocation success is claimed.
