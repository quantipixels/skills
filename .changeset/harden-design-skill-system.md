---
"qp-skills": minor
---

Harden the design system around one lightweight design router and independently useful artifact owners. Make `apere` route broad, ambiguous, or multi-deliverable design work without producing artifacts or owning delivery lifecycle; use `alaga` for integrated production after routing. Move logo, corporate-identity, and custom-icon visual-language resources and search data from `apere` to `brand`, while `asa-oju-ibanisoro` owns product icon implementation. Extract the former social-graphics mode into the new lightweight `social-graphics` skill for feed posts, carousels, stories, templates, and multi-format campaigns, leaving `banner-design` focused on banner-like display surfaces. Keep `amoye-ui-ux` as the owner of one composable, evidence-backed visual-direction result, with durable state owned by the calling workflow. Keep `eto-apere` as the sole owner of canonical tokens, component specifications, generated configuration, and token migrations. Preserve canonical aliases and names, complete linked or embedded slide token contracts with theme overrides, rejection of undefined slide variables, empty validation scopes, and raw CLI colors. Preserve Brand context and asset validation while removing its duplicate token writer and image-color wrapper. Reconcile Alarina, README, plugin catalog, and design routing without removing existing non-design or experimental routes inherited from the parent PR.
