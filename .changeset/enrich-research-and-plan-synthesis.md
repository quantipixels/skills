---
"qp-skills": minor
---

Add `seda-spec` as the standalone owner of confirmed implementation-independent behavior specifications. Let `atona` consume a ready specification when material behavior needs a separate contract, then preserve the contract through optional ticket decomposition and implementation. Enrich `iwadi` with a noise-aware delegation threshold and compact primary-source evidence packets.
