---
"qp-skills": minor
---

Add `seda-spec` as the standalone owner of confirmed implementation-independent behavior specifications, including explicit placement and retirement rules. Let `atona` consume a ready specification, preserve it through optional decomposition and implementation, and promote durable lifecycle knowledge into versioned project sources of truth before closure. Enrich `iwadi` with a noise-aware delegation threshold and compact primary-source evidence packets.
