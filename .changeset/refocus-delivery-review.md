---
"qp-skills": minor
---

Restore crisp coding ownership without a breaking removal: Alága now selects for accepted code delivery while explicit legacy `scope-only` invocation remains compatibility-only. Àtúnwò selects for independent code judgment with `change`, `codebase`, and `parity` modes; change review isolates contract/spec, engineering standards, proof, and bug-hunt axes before reconciliation, with codebase-specific depth loaded selectively.
