---
"qp-skills": minor
---

Make prose cleanup an independently selected outcome instead of a mandatory suffix to technical writing, preserving `technical-writing` for structure and clarity and `yo-slop` for final cleanup or explicit pruning when that result is actually useful.
