---
"qp-skills": patch
---

Consolidate native Codex rejection tests into package integrity checks. Keep shipped safety mechanics in package CI and reserve standing model-evaluation corpora and harnesses for the separate internal eval repository. Remove the duplicate in-package evaluation scenario catalogue without changing skill behavior or runtime verification claims.
