---
"qp-skills": patch
---

Clarify test-first implementation as a proof technique within `alaga` delivery, system and change inspection as scopes of `pare`, defect-only requests as review restrictions in `atunwo`, and pruning as an explicit editing objective in `oro-ologbon`. Preserve read-only boundaries, scope-only delivery restrictions, parity evidence requirements, and existing requests for these behaviors.
