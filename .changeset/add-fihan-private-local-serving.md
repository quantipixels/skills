---
"qp-skills": minor
---

Add explicit-only Experimental `fihan` for temporary tailnet-only access, or explicitly accepted encrypted Tailcat bearer-token access, to bounded local files. Prefer an already-ready Tailscale Serve route and use Tailcat only when the receiver accepts its CLI, capability-token, relay-metadata, and stability boundaries.
