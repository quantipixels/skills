---
"qp-skills": patch
---

Embed the Quantipixels logo directly in the HTML Artifact base template so standalone artifacts keep their brand mark without a companion `logo.svg` file. Update the skill guidance and browser proof to enforce the self-contained logo contract.
