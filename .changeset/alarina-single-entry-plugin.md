---
"qp-skills": major
---

Consolidate every QP skill and utility into one canonical Alárinà skill. Retire the standalone entrypoints, split distinct methods into focused internal commands (including oro-eniyan, oro-sigidi, seda-spec and seda-tickets), and preserve resources and invocation boundaries with their references.

Compile provider-specific Codex and Claude plugins with exact namespaces, implicit discovery metadata, lazy command loading, and a Claude agent profile using the same operating method. Add deterministic packaging, provenance, migration checks and separate discovery, dispatch/loading and behavior evaluation lanes. Existing selective installations must migrate to Alárinà; generated caches and unrelated local skills are not edited automatically.

Require current local review and relevant checks before publishing substantive candidates, reusing delivery evidence and batching corrections. Add optional project/environment setup and bounded command composition, including applying Àròjinlẹ̀ with the agent as its respondent. Reduce routine artifacts, make HTML plans conditional, and keep resumable private state in user-level storage while preserving existing records and shared project knowledge.

Project setup and broad documentation-improvement requests establish useful baseline coverage, creating missing architecture, standards, confirmed non-goals and working instructions through their existing owners while preserving scoped audits and edits.

Group references by use case with a conditional discovery guide. Compose commands, references and available capabilities around uncovered outcomes when no predefined method is sufficient. Share the agent-respondent perspective, premise checks and knowledge-discoverability guidance across commands, preserving each method's authority and completion boundaries. Keep inspiration acknowledgements in the README rather than command and reference instructions.

Retain useful custom workflows supported by real execution in the optional user-level `~/.qp/alarina/ona/` library through retrospectives. Discover and adapt relevant saved paths when composing a task, preserving evidence, applicability, explicit read-only scope and current authority.

Expand project setup to discover and confirm project philosophy with Àròjinlẹ̀, and adapt the full SDLC from framing through operation, recovery and learning. Share guidance with planning, delivery and retrospectives for proportional proof, reversible experimentation, useful capabilities and codification at the appropriate owner.
