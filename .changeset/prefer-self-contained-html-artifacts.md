---
"qp-skills": patch
---

Make standalone HTML Artifact output self-contained by default: prefer native HTML/CSS/embedded JavaScript, bundle focused executable dependencies when justified, reuse an existing host application's runtime only when it already owns the artifact, and require explicit network-backed need plus deep runtime/failure proof before introducing remote executable code or live services.
