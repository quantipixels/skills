---
"qp-skills": minor
---

Add `seda-pr`, `wo-pr`, and `tunmo-pr` as focused GitHub PR and GitLab MR skills for ready-for-review zero-context publication, active pipeline and evidence-backed feedback stewardship, and read-only first-reviewer explanation. Include dual-provider watcher normalization, crash-safe state, bounded retries, leases, authority gates, configurable readiness settlement, and routing and evaluation coverage. A bare monitoring request gives `wo-pr` bounded authority to validate and fix branch-caused blockers and update the PR until it is ready for the user's merge decision; approval and merge remain excluded. `seda-pr` never creates a draft item on any provider.
