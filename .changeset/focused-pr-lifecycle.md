---
"qp-skills": minor
---

Add `seda-pr`, `wo-pr`, and `tunmo-pr` as focused GitHub PR and GitLab MR skills for zero-context publication, active pipeline and feedback stewardship, and read-only first-reviewer explanation. Include dual-provider watcher normalization, crash-safe state, bounded retries, leases, authority gates, and routing and evaluation coverage. A bare monitoring request gives `wo-pr` bounded authority to fix branch-caused blockers and update the PR until it is ready for the user's merge decision; approval and merge remain excluded.
