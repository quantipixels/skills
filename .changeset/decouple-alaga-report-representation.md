---
"qp-skills": patch
---

Keep Alága's job-report gate and semantic integration responsibility while removing HTML as the automatic report representation. Return the report directly by default; use `html-artifact` only when a visual projection materially improves the handoff/review or is explicitly requested, and preserve existing persistence ownership when a durable file is actually needed.
