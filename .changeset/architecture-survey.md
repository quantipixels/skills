---
"qp-skills": minor
---

Add an Architect survey mode that finds and ranks evidence-backed architectural friction before a design is chosen. Focus broad surveys with bounded change history when no target is supplied, use deep-module and ownership signals as leads rather than scores, and stop at candidate selection so discovery does not silently become redesign.
