# Direct-owner evidence selection

Treat each request below as a separate direct invocation of the named skill, using the supplied frozen skill directory. Do not bootstrap through Alárinà. Supporting installed skills in that directory are available. This is a selection probe: return the next useful operation, its owner, required evidence, and stopping or authority limit for each request. Do not execute the hypothetical work or install anything.

1. **Architect, design:** Choose the boundary for a Java service split. The build declares an interface dependency, but version substitution and generated sources may change its actual consumers. A resolved build graph and IDE symbol navigation are available. The user wants a design recommendation, without code changes or a preliminary repository survey.
2. **Àtúnwò, review:** A Kotlin change calls `publish(receipt)`. Two overloads have different persistence effects, and text matches do not resolve the receiver. IDE navigation is available. Receipt assertions may accept swapped fields. Source/Git edits and test execution are prohibited; no mutation results were supplied.
3. **Atọ́nà, behavior contract:** Derive refund behavior from a supplied workbook and provider-policy export. The workbook has formulas and hidden rows; the export has an unconsumed next-page token. A spreadsheet reader and authorized read-only provider access are available. Return only the behavior contract; do not start delivery.
4. **Àdánwò, measured experiment:** Decide whether to keep a build-cache change. The candidate has a faster warm no-op build; clean builds, changed-source invalidation and remote work were not measured. Native build tracing is available. Trial edits and bounded local runs are authorized; new dependencies and publication are not.
5. **Ìwádìí, research:** Compare current provider retry guarantees. One official source is browser-rendered; another exposes paginated structured records. Browser and read-only API access are available. No version-specific source-code question exists. Produce an evidence-backed conclusion.
6. **Amọ̀ṣẹ, knowledge reconciliation:** A default search found no prior rule, but the relevant knowledge store is ignored by that search and includes structured records. Read access is available. Determine whether a proposed rule conflicts with an established one; do not write the rule yet.
7. **Yorùbá glossary, authorized update:** Update one accepted mapping in a semicolon CSV with quoted multiline descriptions. Text search finds the English term inside another entry's description. Preserve unrelated entries and the existing schema. A native CSV parser is available.
8. **Wò PR, status only:** Checks passed for the current head. The provider response includes the first page of review threads and one thread has more comments. Some details may require additional read access. Report readiness without editing, replying, resolving, publishing or merging.
9. **Àyẹ̀wò, postmortem:** Explain failed settlement retries from a JSONL export. A text search reports many occurrences of `error`, including quoted request bodies; the last tool response was truncated. A native JSON parser and the original local export are available. Keep source logs unchanged and separate supported cause from missing evidence.
10. **Alága, ordinary delivery control:** Correct a settled one-line configuration typo in a known short file. The existing native check covers the changed behavior and is authorized. No navigation, build-resolution or tool-readiness uncertainty exists.
11. **Alága, delivery:** Implement an accepted build configuration change that updates a generator dependency. The warm build passes, but the generated client still appears to use the old schema. Resolved dependency output and native build-action tracing are available. Scoped configuration/source changes and targeted local checks are authorized; installation and publication are not.
12. **Alága, diagnosis follow-up:** A VAT defect is confirmed: one path treats a VAT-inclusive amount as net and applies the tax again. Find variants in the related billing paths. The known instance is fixed on the current branch but available at the preceding revision. Entry points use different names and wrappers; structural and symbol navigation are available. Keep this follow-up read-only and bounded to the same causal mechanism.
