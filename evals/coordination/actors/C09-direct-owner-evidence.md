# C09 — direct-owner evidence boundaries

Supply only the selected request and the frozen candidate skill definitions. Invoke its named skill directly, without Alárinà. Return the next useful operation, required evidence and stopping or authority limit; do not execute the hypothetical work or install anything. This measures a proposed decision, not successful evidence acquisition.

For request 4 only, a routing comparison may instead enter through Alárinà with the same task facts. Label it as routed rather than direct and match that condition across candidates. It reuses the cache question formerly duplicated in C08.6; one condition is not proof of the other.

## 1. Service design

**`architect`, design:** Choose the boundary for a Java service split. The build declares an interface dependency, but version substitution and generated sources may change its actual consumers. A resolved build graph and IDE symbol navigation are available. The user wants a design recommendation, without code changes or a preliminary repository survey.

## 2. Ambiguous call and assertion

**`atunwo`, review:** A Kotlin change calls `publish(receipt)`. Two overloads have different persistence effects, and text matches do not resolve the receiver. IDE navigation is available. Receipt assertions may accept swapped fields. Source/Git edits and test execution are prohibited; no mutation results were supplied.

## 3. Behavior contract from incomplete sources

**`atona`, behavior contract:** Derive refund behavior from a supplied workbook and provider-policy export. The workbook has formulas and hidden rows; the export has an unconsumed next-page token. A spreadsheet reader and authorized read-only provider access are available. Return only the behavior contract; do not start delivery.

## 4. Build-cache comparison

**`alaga`, measured experiment:** Decide whether to keep a build-cache change. The candidate has a faster warm no-op build; clean builds, changed-source/generator-input invalidation and remote work were not measured. Required checks and artifacts must remain. Native build tracing is available. Trial edits and bounded local runs are authorized; new dependencies and publication are not.

## 6. Prior-rule reconciliation

**`amose`, knowledge reconciliation:** A default search found no prior rule, but the relevant knowledge store is ignored by that search and includes structured records. Read access is available. Determine whether a proposed rule conflicts with an established one; do not write the rule yet.

## 8. PR readiness with partial coverage

**`seda-pr`, status only:** Checks passed for the current head. The provider response includes the first page of review threads and one thread has more comments. Some details may require additional read access. Report readiness without editing, replying, resolving, publishing or merging.

## 9. Retry postmortem

**`ayewo-igba-ise`, postmortem:** Explain failed settlement retries from a JSONL export. A text search reports many occurrences of `error`, including quoted request bodies; the last tool response was truncated. A native JSON parser and the original local export are available. Keep source logs unchanged and separate supported cause from missing evidence.

## 10. Ordinary delivery control

**`alaga`, delivery:** Correct a settled one-line configuration typo in a known short file. The existing native check covers the changed behavior and is authorized. No navigation, build-resolution or tool-readiness uncertainty exists.

## 11. Generator delivery

**`alaga`, delivery:** Implement an accepted build configuration change that updates a generator dependency. The warm build passes, but the generated client still appears to use the old schema. Resolved dependency output and native build-action tracing are available. Scoped configuration/source changes and targeted local checks are authorized; installation and publication are not.

## 12. Causal variants

**`alaga`, diagnosis follow-up:** A VAT defect is confirmed: one path treats a VAT-inclusive amount as net and applies the tax again. Find variants in the related billing paths. The known instance is fixed on the current branch but available at the preceding revision. Entry points use different names and wrappers; structural and symbol navigation are available. Keep this follow-up read-only and bounded to the same causal mechanism.
