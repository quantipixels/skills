# C14 — native profile admission and limits

**Observation:** selection probe; separately authorized native replication  
**Instruction source:** supplied candidate  
**Workspace:** none for the probe

Use the supplied `codex-orchestra` and relevant caller definitions. Treat each record as a separate hypothetical condition. Propose the outcome and evidence limit; do not launch workers or change settings in this probe.

1. Codex/Astra root, useful separable assignment, compatible native Luna model and effort controls.
2. The user says no subagents; a review requires independence from the root's own implementation.
3. The user requests Luna-only subagents; the task could use a separately warranted specialist judgment.
4. A named custom worker pins a model outside the profile; a compatible general worker is available with established settings.
5. The same pin conflicts, but no compatible path or effective model metadata can be established.
6. An allowed worker needs more reasoning. The host has no in-place effort adjustment. Useful partial evidence exists.
7. Two QP children are already live, including review; another proposed assignment is useful but not urgent.
8. A non-Astra root or a non-Codex host encounters this skill. Separately, a selective install contains the needed specialist but not this profile.
9. One short successful worker result is offered as proof of quota savings and native discovery. No parent/context/retry accounting or discovery trace exists.

For a native replication, the evaluator must first supply a finite authorized trial count/resource ceiling, exact candidate, compatible target-host capability evidence, isolation boundary and stop conditions. Run only authorized compatible paths; conflicting or forbidden model conditions remain supplied-record probes. Preserve requested and resolved metadata separately, actual actions/artifacts and available traces. No installation, configuration change, live forbidden-model arm, or automatic retry is authorized by this case.
