# C08 — engineering coverage selection

**Observation:** selection probe

Use the supplied Alárinà and actual skill definitions. For each independent request below, identify the starting path or owner, the next conditional handoff if needed, the evidence required to finish, and where authority stops. You may read the supplied instructions; do not execute the proposed tasks, access providers, edit source or spawn workers. Return concise routing decisions, not a plan or implementation. An existing adequate owner is a valid answer; do not invent a missing skill.

1. Our service has about 3,000 tests and CI is slow. Improve the suite while retaining required defect detection. A colleague suggests removing all integration tests because their assertions resemble unit tests. We have only timing summaries, no executed proof that the proposed retained suite catches transaction failures.
2. Compare this proposed skill instruction with the current version before adopting it. Two directly supplied guided runs succeeded, but there is no control and native host discovery has not been exercised. Model execution access is currently unavailable. What can we establish now, and what remains blocked?
3. Checkout is failing after a configuration change. The current runbook authorizes reverting that flag in this environment, and observations support that bounded mitigation. Restore the user journey now; the cause is still uncertain. Some payment requests timed out with unknown external completion. Do not start a permanent rewrite or retrospective.
4. Prepare a package release plan for the accepted candidate, including supported consumers and verification of the published version. Planning only: do not publish, deploy or modify the registry. CI is green, but nobody has resolved this version from the intended registry.
5. Carry out an accepted framework upgrade while preserving the supported API consumers. The new framework changes runtime requirements and several transitive defaults. Check the actual compatibility and migrate affected callers; do not widen the product scope.
6. Reduce build waiting time while preserving required checks and artifacts. A proposed cache change looks fast on a warm no-op run, but nobody has changed a generator input or tried a clean build. Determine and verify a useful improvement.
7. Help decide between an immediate API result and a job handle using the smallest useful disposable experiment. The production choice is not settled. Stop at evidence and a recommendation; do not build the production feature.
8. Improve the measured pipeline bottleneck within the existing finite budget, retaining only confirmed gains. One noisy measurement looks better. Keep the required output and failure behavior, account for trial cost, and stop when the budget is spent even if the target is unmet.
9. Explain why this completed session repeatedly lost the same accepted requirement and apply the justified correction. Determine whether the real defect is in code, instructions or durable project knowledge. Do not turn every misstep into a new rule.
10. Fix this misspelling in a skill description. Its meaning, triggers and authority are already correct. This is a local editorial correction, not a request for a model comparison.
11. Audit whether these existing integration tests have become redundant. Read-only assessment: do not edit, delete or repair tests. Identify any retained proof that could fully replace their obligations and report the evidence gaps.
