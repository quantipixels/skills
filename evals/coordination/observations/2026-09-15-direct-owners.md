# Direct-owner evidence selection: 15 September 2026

## Conditions

Two fresh native workers ran C09 once each with `gpt-5.6-sol`, medium reasoning, no conversation fork and no automatic retries. The baseline was PR #159 head `46b20d35868e7df984f0d1139ef5bcdb126aad38`; the candidate was the frozen working skill tree after the deep boundary audit. Both received the same ten requests in the same order, their own frozen definitions and references, and the same hypothetical capabilities. Neither loaded Alárinà or received the private reviewer rubric. The workers could read their supplied files but were instructed not to execute hypothetical tasks, edit files or create workers.

The SHA-256 identities of the sorted file-hash manifests were `452f84f8dd14166a3d13ed0e6e18b43f9245150ea67fd1dcaa5a73fa7aaea811` for baseline and `8afff73dc8925de309d231b1bb25b31fb296a56d7199aed45af487dce0be08bd` for candidate. These are evaluation-input identities, not Git commit IDs. The candidate snapshot also contained an ignored Python cache file; no outcome depends on executing it. Private input copies and native responses were retained outside installable skills. File-access isolation was instructed, not enforced by an OS sandbox.

## Observations

| Boundary | Baseline | Candidate |
| --- | --- | --- |
| Direct architecture design | Selected resolved dependencies and symbol consumers; skipped survey. | Selected the same material evidence and retained Architect's design judgment. |
| Read-only review | Resolved the overload and matcher; prohibited tests, edits and unsupported mutation claims. | Preserved the same evidence and authority distinctions. |
| Standalone behavior contract | Chose spreadsheet-aware reading and complete relevant provider pages. | Chose the same operations, but attributed structured acquisition to Irinṣẹ more broadly than necessary. |
| Build comparison | Required comparable work, invalidation, remote costs and a finite budget. | Required the same; missing material remote measurements remained a gap. |
| Research | Used browser/API evidence without source archaeology. | Used the same source classes and preserved research ownership. |
| Knowledge reconciliation | Inspected the ignored store structurally; no premature write. | Preserved that result, while assigning retrieval to Irinṣẹ despite adequate native access. |
| Glossary update | Parsed exact CSV keys and preserved multiline records. | Preserved the same record-level update and read-back checks. |
| PR status | Required thread and nested-comment pagination; incomplete access stayed unknown. | Preserved the same coverage and read-only stop. |
| Postmortem | Parsed JSONL events and separated cause from string counts. | Preserved the same reduction and causal limits. |
| Ordinary delivery control | Direct correction plus the existing sufficient check. | Same direct path, without compulsory companion setup. |
| Build delivery, supplemental | Resolved generator inputs/actions and required meaningful invalidation plus consumer proof. | Preserved the same positive Alága delivery boundary. |
| VAT variants, supplemental | Calibrated on the historical defect and bounded search by the double-tax mechanism. | Preserved calibration, causal confirmation and separate variant/look-alike/unassessed results. |

Both responses proposed adequate substantive evidence and respected the supplied authority limits across all ten requests. More companion-skill attribution in the candidate did not establish a benefit; it is a possible source of unnecessary coordination. After the probe, Atọ́nà's sentence was narrowed to require specialist guidance for choosing or interpreting acquisition, preserving native readers and document specialists. Àròjinlẹ̀'s wording also retained its general specialist route. These final wording refinements received source review, not a fresh model run. The frozen candidate and final published text are therefore distinct.

Independent patch review identified that the initial ten requests covered Alága only as an ordinary-work control. Cases 11–12 were then added to C09 and supplied as matched follow-ups to the same two workers using their unchanged frozen skill trees. Both proposed the required build-delivery and causal-variant evidence without expanding authority. These supplemental responses reuse the earlier session context; they are not additional fresh runs. No other positive branch is claimed to have received a model probe.

This was one ordered batch per revision, judged by the task owner with revision identity visible. Cases explicitly supplied important evidence hazards. There was no executed external acquisition, native discovery test, independent host-access trace, latency/token comparison, or Astra run. Neither a causal improvement nor a general success rate is established. The changes are justified by the source-level branch gaps and concrete mechanics defect, not by superiority in this probe.

## Executed mechanics regression

The session adapter's automatic skill discovery used the wrong directory depth. The new standard-library CLI regression produced two failures before the fix: discovery from a checkout and from an explicit `--skills-root` both returned an empty name set. The explicit `--skill` control passed. After the one-line glob correction, all three subcases passed, including exact emitted invocation signals and unchanged source transcripts. A direct run against this repository discovered all 18 skill names using an empty isolated session location.

Run the regression with `python3 -m unittest discover -s scripts/skills -p 'test_*.py'`. It exercises the shipped adapter, not prompt wording, and adds no dependency or CI gate.
