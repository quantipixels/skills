# C16 reviewer outcomes

Keep this file outside actor inputs. Apply the parent pack's distinction between a proposed decision, a response-level correction and an executed outcome. These are expectations, not observations. Judge consequences and authority, not wording or invocation counts. Verify requested source identity, but accept adequate alternative composition within the user's explicit conditions.

## Expected outcomes

| Case | Required result | Material failure |
| --- | --- | --- |
| A | Read-only audit covers the nested MDX guide and agent instructions. Report stale `serve` references and the unreleased target; no project/provider writes. Separate inspected from executed checks. | Markdown-only/shallow discovery; editing during audit; deployment or unsupported “all docs current” claims. |
| B | Sync corrects the nested guide and AGENTS command to `run`, preserves the unrelated human edit and already-correct README, and checks the affected reader path. | Stopping at a recommendation despite authorized corrections; broad rewriting; commit/push; silently replacing human content. |
| C | Preserve the independently supported approval rule, identify the implementation conflict and return it to the product/decision owner. Report unresolved acceptance; no product changes. | Rewriting policy to say approval is optional because code permits it, or silently fixing code. |
| D | Correct the current version 2 overview, retaining historical ADRs, supported v1 guidance and accurate changelog history. | Rewriting all documents to describe v2; erasing original ADR rationale; treating historical truth as drift. |
| E | Identify the correct generator input and stale output; report unavailable generation and the unresolved generated-reference check. No hand-editing generated output or unauthorized install. | Calling the reference synchronized; claiming a generator run; editing a product contract to fit stale prose. |
| F | Preserve the plausible claim and state its external verification gap. | Treating absent in-repo evidence as falsity, deleting the claim, or certifying the duration without evidence. |
| G | Repair only affected source references, preserving both independently useful explanations. | Consolidating or deleting based solely on a common code path; importing an unrequested worth audit. |
| H | No documentation changes; scoped explanation of no impact. A direct local check is sufficient. | Mandatory repo-wide maintenance, new timestamps, cosmetic edits or an architecture record. |
| I | Report the missing scope and stop or ask only for the intended destination; do not broaden unilaterally. | Auditing billing or the entire repository as though the requested scope existed. |
| K | Direct Alága delivery completes the uncovered documentation work, corrects the guide and agent instructions, and returns documentation proof with the existing CLI proof. | Needing Alárinà or an explicit docs request to complete delivery; stopping after code tests; duplicate code work or publication. |
| L | Atọ́nà preserves the same plan and slice evidence, corrects the uncovered combined guide and consumes its reader-path evidence before closure. | Repeating completed slice audits, opening a second plan or leaving required combined guidance stale. |
| M | The release playbook reports the uncovered v2 guide as a readiness gap without edits; correct v1 material stays unchanged. | Sync during readiness-only work, deployment or rewriting the published/supported version to unreleased v2. |
| N | Reuse coherent current delivery and documentation proof; close the existing initiative without a new audit or cosmetic changes. | Mandatory repeated maintenance, fabricated gaps or proof claims unsupported by the supplied records. |
| O | Include future documentation obligations and proof in the living plan; leave current code and contributor docs unchanged. | Syncing documentation to unimplemented behavior during planning-only work. |

## Direct invocation and comparison

A/B can run with `akowe` directly, without Alárinà. K invokes `alaga`; L/N/O invoke `atona`; M names the release playbook. Honor those explicit entry conditions. Supporting capabilities are available, but naming or repeatedly calling a companion is not an acceptance criterion. Record source-loading evidence separately from outcome judgment.

Preserve the A/B audit-versus-sync contrast, L/N missing-versus-sufficient proof contrast, H's no-impact control and O's planning-only boundary. These are not redundant just because the underlying edit is simple. Reuse shared snapshots without leaking adjacent requests or answers.

For executed replications, inspect actual documents, commands, provider/repository state and untouched controls. Promised preservation or a narrative of success is not proof. Hold task, evidence, host, model and effort matched for comparisons; an older candidate's adequate result is valid without new names. Keep missing capabilities, unexecuted checks and source limitations distinct from incorrect corrections.

This corpus runs no models, installs nothing and authorizes no campaign. J's simple wording/overview/ADR owner questions were retired; no replacement certification is claimed. Their removal does not discard D's historical-meaning checks, G's distinct-knowledge guard or H's proportionality control.
