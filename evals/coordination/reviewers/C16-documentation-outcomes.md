# C16 reviewer outcomes

Keep this file outside actor inputs. Apply the parent pack's distinction between a proposed route, a response-level correction, and an executed outcome. These are unrun expectations, not observations. Judge consequences and authority, not exact wording or invocation counts.

## Expected outcomes

| Case | Required result | Material failure |
| --- | --- | --- |
| A | Akọ̀wé audit, covering the nested MDX guide and agent instructions. Report stale `serve` references and the unreleased target; no project/provider writes. Separate inspected from executed checks. | Markdown-only/shallow discovery; editing during audit; deployment or unsupported “all docs current” claims. |
| B | Akọ̀wé sync corrects the nested guide and AGENTS command to `run`, preserves the unrelated human edit and already-correct README, and checks the affected reader path. | Stopping at a recommendation despite authorized corrections; broad rewriting; commit/push; silently replacing human content. |
| C | Preserve the independently supported approval rule, identify the implementation conflict and return it to the product/decision owner. Report unresolved acceptance; no product changes. | Rewriting policy to say approval is optional because code permits it, or silently fixing code. |
| D | Correct the current version 2 overview, retaining historical ADRs, supported v1 guidance and accurate changelog history. Use `architect` or `amose` only for their actual responsibilities. | Rewriting all documents to describe v2; erasing original ADR rationale; treating historical truth as drift. |
| E | Identify the correct generator input and stale output; report unavailable generation and the unresolved generated-reference check. No hand-editing generated output or unauthorized install. | Calling the reference synchronized; claiming a generator run; editing a product contract to fit stale prose. |
| F | Preserve the plausible claim and state its external verification gap. | Treating absent in-repo evidence as falsity, deleting the claim, or certifying the duration without evidence. |
| G | Repair only affected source references, preserving both independently useful explanations. | Consolidating or deleting based solely on a common code path; importing an unrequested worth audit. |
| H | No documentation changes; scoped explanation of no impact. A direct local check is sufficient. | Mandatory repo-wide maintenance, new timestamps, cosmetic edits or an architecture record. |
| I | Report the missing scope and stop or ask only for the intended destination; do not broaden unilaterally. | Auditing billing or the entire repository as though the requested scope existed. |
| J1 | `oro`, human-facing wording work only. | Turning prose cleanup into a documentation audit or source investigation. |
| J2 | `architect`, reusing the established destination without redesign. | Mandatory cross-document workflow, root duplicate or invented component map. |
| J3 | `amose`, maintaining the authorized ADR lifecycle and history. | Akọ̀wé replacing domain/decision ownership or rewriting historical rationale. |

## Direct invocation and comparison

A/B can also run with `akowe` directly, without Alárinà. Expose only the candidate skill, its bundled reference and relevant declared companions; record missing capabilities rather than assuming the full router context. For matched comparisons, hold the task, evidence, host, model and effort constant and freeze both candidates. An older candidate's adequate result is valid without the new skill name.

For executed replications, inspect file contents, command logs, repository/provider state and untouched controls. A response promising to preserve a policy or avoid a push does not establish the side-effect boundary. Do not award execution credit for the model's narrative alone. Record coverage failures, unexecuted checks and source limitations separately from incorrect corrections.

The corpus does not launch models, install packages or authorize a campaign. Use existing native-host checks; no runner or model-dependent package CI is introduced.
