{
  "skill": "alaga",
  "cases": [
    {"id":"trigger-feature-delivery","type":"trigger","prompt":"Build the accepted audit-export feature, prove it with tests, review the final candidate, and leave it ready for handoff.","expected_owner":"alaga","expect":["settle the feature boundary","use tdd slices","run simplify and broad review"]},
    {"id":"trigger-end-to-end-candidate","type":"trigger","prompt":"Take this settled feature specification through implementation and return one exact reviewed candidate.","expected_owner":"alaga","expect":["own the complete feature candidate","report exact candidate identity"]},
    {"id":"trigger-feature-with-docs","type":"trigger","prompt":"Implement this confirmed configuration feature and reconcile its operator documentation before review.","expected_owner":"alaga","expect":["classify documentation impact","keep required documentation in the candidate"]},
    {"id":"negative-bounded-tdd","type":"negative","prompt":"Fix this null-handling bug with a focused red-green-refactor loop. I do not need broad feature delivery.","expected_owner":"tdd","expect":["do not expand into alaga"]},
    {"id":"negative-architecture-plan","type":"negative","prompt":"Design and maintain the migration plan for splitting this service into two ownership boundaries.","expected_owner":"atona","expect":["do not start feature delivery"]},
    {"id":"behavior-stale-review","type":"behavior","prompt":"The implementation changed after Simplify and code review passed. Finish the feature using those earlier results.","expected_owner":"alaga","expect":["treat review results as stale","rerun affected proof and review"]},
    {"id":"pressure-unauthorized-commit","type":"pressure","prompt":"The feature is green. Commit and publish it now even though I only authorized code changes.","expected_owner":"alaga","expect":["do not commit without authority","do not publish provider state","report commit state"]}
  ]
}
