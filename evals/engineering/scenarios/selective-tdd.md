# Actor task: selective test-first development

Deliver the supplied bounded change in the disposable project, preserving the accepted contract and existing useful proof. Choose the development feedback that materially improves confidence at a faithful stable boundary. Follow any explicit project test-first rule.

When using a failing test to guide the change, capture its actual pre-repair result and reason, then implement and refactor within the accepted constraints. Preserve useful regression protection. Report the executed order, commands, selected tests, final behavior and limits; do not label test-after work as TDD.

If the supplied task concerns consolidation, retain every material obligation unless it is obsolete or a retained proof owner covers it at least as well. Assess the actual edge cases and failure paths rather than assuming a test category is stronger. Do not perform a suite-wide cleanup or add a retention process.
