Fix chunks in batching.py: it currently loses items when the input length is not divisible by the batch size. For a list and positive integer size, return consecutive nonempty batches preserving all items and order; the final batch may be shorter. Empty input returns []. Preserve ValueError for nonpositive sizes and do not mutate the input. Keep the public API and existing Python standard-library setup. Leave a focused regression test and explain the cause, proof and limitations in RESULT.md. Run tests with python3 -m unittest discover -v.
