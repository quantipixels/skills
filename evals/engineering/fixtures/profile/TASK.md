Diagnose the supplied synthetic sampled-stack artifact and correct diagnosis.json. The artifact is a controlled interpretation fixture, not output from a native profiler session. Do not modify capture.json or prescribe a code fix. The returned JSON must identify the capture and conditions, report the total weighted samples, reduce the relevant inclusive path with symbol/file/line/sample attribution, separate observations from causal claims, and name the smallest next probe that distinguishes the remaining explanations.

Use these stable machine-readable fields: `capture_id`, `format`, `sample_count`, `capture_conditions`, `reduced_path`, `source_attribution`, `causal_assessment`, and `next_probe`. `causal_assessment` has `status`, `confirmed_root_cause`, and `live_hypotheses`. Allowed statuses are `CONFIRMED_ROOT_CAUSE` when discriminating evidence establishes the mechanism, `DIAGNOSED_BUT_UNPROVED` when the best explanation retains a material causal gap, `EVIDENCE_BLOCKED` when a named evidence gap prevents diagnosis, and `NOT_REPRODUCED` when the pinned failure was not observed. Use `single_large_snapshot` and `repeated_snapshot_parse` for the two live hypotheses. `next_probe` has `scope`, `metrics`, and `distinguishes`; use `parse_calls` and `snapshot_bytes` for the available measurements and name both hypotheses that they distinguish. The artifact can establish where samples accumulated, but it contains neither parse-call counts nor snapshot byte sizes. Preserve that evidence limit: a sampled hotspot or plausible story does not by itself prove a root cause.

Explain the evidence, attribution, alternatives, stopping point and next discriminating probe in RESULT.md. No model, network, application execution, or new profiler capture is needed for this task.
