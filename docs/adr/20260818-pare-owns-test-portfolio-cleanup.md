# Let Parẹ́ own test-portfolio cleanup

Status: Superseded by [Let Pare own simplification and cleanup](20260818-pare-owns-simplification-and-cleanup.md).

QP will keep test-portfolio cleanup inside Parẹ́ instead of publishing another skill. Parẹ́ now exposes `repository-audit` for its existing read-only simplification audit, `test-cleanup` for automated-safe removal of tests whose complete signal has a stronger proof owner, and `test-cleanup-deep` for aggressive contract-focused deletion after one explicit HITL or opt-in authorization. The mode boundary keeps repository audits read-only while giving cleanup one simplification owner, one proof inventory, and one acceptance report.
