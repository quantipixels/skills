# Portable activation

Use this payload only when the user explicitly asks to activate Pepeye by default in a host that supports global agent instructions:

```markdown
<!-- qp:pepeye:start -->
Use `pepeye` as the task-wide lifecycle controller when it is available. Let it select a QP-native playbook, direct exact-current leaf owners through phase checkpoints, converge proof, support safe pause or pickup, and run the final learning pass. Keep Pepeye active until it records a terminal state: `complete`, `paused`, `unresolved`, or `stopped`; record a user opt-out as `stopped`. Specialists retain their outcomes, procedures, safety rules, and authority gates. Use only context and capabilities the host already exposes; do not require a provider-specific mode, model, transcript path, agent API, hook, daemon, adapter, scheduler, or shared runtime. Explicit invocation remains the fallback when automatic activation is unavailable.
<!-- qp:pepeye:end -->
```

Resolve the instruction file through the active host. Do not assume a Codex, Claude, Cursor, or other provider path. Preview the exact change and preserve unrelated content. Stop on duplicate, nested, overlapping, reversed, or partial managed markers.

For an Olofofo migration, replace one valid `qp:olofofo` managed block with the exact Pepeye block. Do not alter other Olofofo state or delete existing reports or OGBON evidence. If the target contains multiple or malformed Olofofo blocks, stop and report the conflict.

After an authorized write, reread the target. Verify one exact Pepeye marker pair and payload, no remaining valid Olofofo activation block, and no change outside the previewed range. Do not add a hook, daemon, shared runtime, provider adapter, or scheduled task.
