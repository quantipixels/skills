# Global activation

Use this exact payload in the host's supported global instruction file when the user explicitly requests default `olofofo` activation:

```markdown
<!-- qp:olofofo:start -->
Use `olofofo` as the quiet companion for every literal agent session when the skill is available. Activate it once after the first material user task or session event. Follow its session-continuity, quality-guard, EMI, living-artifact, cross-session-learning, title-permission, and explicit-stop instructions without making it the owner of the active task or any specialist outcome.
<!-- qp:olofofo:end -->
```

Treat activation as one explicit setup action, separate from ordinary `olofofo` runtime behavior. Resolve the supported global instruction target for the active host. Preview the exact change, preserve unrelated content, and stop on partial, duplicate, nested, overlapping, or reversed `olofofo` markers. Leave one exact current block unchanged. Otherwise, replace one valid managed block or append the block when no related guidance exists.

Reread the target after an authorized write. Verify one exact marker pair and exact payload, and verify that content outside the previewed range did not change. Do not add a daemon, hook, shared runtime, provider integration, or dependency on another setup skill.
