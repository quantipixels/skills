# Slides Reference

Presentation work belongs to the `slides` skill. Use that skill's references and local data/scripts so there is one owner for slide creation, search, validation, and generation.

## Handoff

Invoke `slides` for HTML presentations, pitch decks, slide narrative, layout, charts, or slide-generation tooling. Keep `apere` as the multi-deliverable router when a request also includes other design outputs.
