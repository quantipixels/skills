---
name: human-view
description: Compatibility entrypoint for explicit requests using the former human-view name. The presentation method now lives in fihanmi; use fihanmi for new presentation or show-me requests.
compatibility: Requires the fihanmi skill.
---

# Human View

Read and apply `fihanmi` to the same supplied material, audience, format and authority. This entrypoint carries no separate presentation method.

For selective installations, install `fihanmi` instead. If it is unavailable, report the missing companion rather than claiming to have applied it. Existing invocation permissions are unchanged; forwarding does not authorize publication or other side effects.
