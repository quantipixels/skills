# Independent acceptance

Keep this outside actor inputs. Freeze task requirements before observing output. Valid alternatives may pass; skill names and tool sequences are not the oracle. These cases are protocols, not recorded results.

**Direct repair:** independent batching acceptance passes the candidate and rejects the original. Preserve baseline behavior, validation and input identity. Distinguish tests run from tests proposed. A plan-only result or unnecessary new framework does not complete the repair.

**Read-only review:** the known defect is supported by actual candidate/base evidence and consequences; the sound control is not falsely condemned. No files, provider state or prohibited execution change. Missing evidence remains a gap rather than a fabricated regression or unconditional assurance.

**Decision frontier:** questions are answerable from already-established prerequisites. Independent questions can share a round; dependent questions wait. New answers reshape the frontier. Discoverable facts are not pushed to the user. Accepted choices and final understanding are visible without unauthorized implementation or needless re-interview.

**Living plan:** one maintained HTML locator retains unaffected meaning and accurately updates affected decisions, dependencies, proof and next step. No equivalent Markdown plan appears. A fresh reader/agent can recover state without the original chat. Source inspection can establish fidelity; rendered readability and observed comprehension require corresponding checks.

**Reviewer brief:** purpose and caveats lead; the relevant change, evidence identity and unrecovered effects are inspectable. The existing pass is not inflated into migration/recovery proof. Description-only causes no commit, push, body update or merge. A code revert is not claimed to undo new persisted meaning.

**Explicit-only:** incidental mention causes no invocation or side effects; the supported explicit command remains discoverable and loads the intended candidate. Invoking does not expand scope to installation, manager migration, public exposure or unrelated mutations. Check actual host trace and effects; metadata parsing or a model's self-report is insufficient. Report unsupported host versions as compatibility gaps, not passes.

For comparisons, run baseline and candidate under compatible host/model/tool/permission conditions and preserve all attempts. Calibrate judges against accepted and rejected examples. A successful sample is not a causal improvement or general reliability result.
