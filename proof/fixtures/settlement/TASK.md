Repair settlement retry behavior and leave focused regression tests. A settlement ID identifies one business operation and its fixed integer-cent amount. Repeating that operation, including retry after an uncertain reply and service/provider reopen, must not create another provider transfer. Reusing its ID with a different amount must raise ValueError. A fresh settlement ID remains an independent operation. Use the supplied provider contract; leave provider.py unchanged. Propagate an uncertain reply so the caller can retry. This fixture models sequential retries, not simultaneous invocations. Preserve already-completed operations stored by the existing implementation when opening an existing database. Use the existing Python standard-library setup. Explain the cause, proof and limitations in RESULT.md.

Run tests with: python3 -m unittest discover -v
